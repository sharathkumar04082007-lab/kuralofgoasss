FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir fastapi uvicorn

COPY backend/demo_app.py /app/demo_app.py

CMD ["sh", "-c", "uvicorn demo_app:app --host 0.0.0.0 --port ${PORT:-10000}"]
